{
  "name": "common",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "hardhat": "^2.3.0"
  },
  "scripts": {
    "lint:fix": "node ../../node_modules/prettier/bin-prettier.js --write \"./**/*.{js,ts}\" && yarn lint --fix",
    "lint": "node -r ts-node/register/transpile-only ../../node_modules/tslint/bin/tslint --config tslint.json --project ./tsconfig.json",
    "compile": "tsc --noEmit",
    "build": "tsc -b"
  }
}
