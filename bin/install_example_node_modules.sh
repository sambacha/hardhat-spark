#!/bin/sh

cd packages/example-projects || return
for f in *; do
    if [ -d "$f" ]; then
        cd $f || return
        echo "Installing packages for $f"
        yarn install
        echo "Finished installing packages for $f"
        cd ..
    fi
done
