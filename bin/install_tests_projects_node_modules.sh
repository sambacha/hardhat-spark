#!/bin/sh

cd packages/e2e-tests/test/projects-scenarios
for f in *; do
    if [ -d "$f" ]; then
        cd $f
        echo "Installing packages for $f"
        yarn install
        echo "Finished installing packages for $f"
        cd ..
    fi
done
